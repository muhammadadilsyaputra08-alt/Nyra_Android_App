org.gradle.jvmargs=-Xmx4096m
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
# HF_MANIFEST_URL is normally injected by CI (see .github/workflows/build.yml).
# Points at the manifest for the character-AI model (Qwen2.5-3B-Instruct LoRA
# fine-tune). Generate this file with generate_manifest.py before building —
# the Unsloth GGUF export doesn't produce it automatically.
HF_MANIFEST_URL=https://huggingface.co/4dil/qwen2.5-3b-fcgr-lora-gguf/resolve/main/model_manifest.json
