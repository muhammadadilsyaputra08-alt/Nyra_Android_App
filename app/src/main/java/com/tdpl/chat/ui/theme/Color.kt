package com.tdpl.chat.ui.theme

import androidx.compose.ui.graphics.Color

// Ink / graphite base — deliberately not pure black, gives depth under OLED
val InkVoid = Color(0xFF0B0C0E)
val InkSurface = Color(0xFF15171A)
val InkSurfaceRaised = Color(0xFF1D2024)
val InkBorder = Color(0xFF2A2E33)

// Ember accent — warm, distinctive, avoids default Material purple/blue
val EmberCore = Color(0xFFFF7A45)
val EmberSoft = Color(0xFFFFB088)
val EmberDim = Color(0xFF7A3D24)

// Signal — cool counterpoint for the model's own messages
val SignalCore = Color(0xFF5EE6C7)
val SignalDim = Color(0xFF1F4A42)

val TextPrimary = Color(0xFFEDEEF0)
val TextSecondary = Color(0xFF9A9EA6)
val TextTertiary = Color(0xFF5C6067)

val DangerCore = Color(0xFFFF5D5D)
